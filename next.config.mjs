/** @type {import('next').NextConfig} */
const nextConfig = {};

export default nextConfig;

module.exports = {
    eslint: {
      // Disable all ESLint rules
      ignoreDuringBuilds: true,
    },
  };